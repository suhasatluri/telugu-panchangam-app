/**
 * Project-wide constants. Centralised here so a future domain change
 * is one edit instead of grep-and-replace across the codebase.
 */

/** Public canonical URL of the live app. */
export const APP_URL = "https://telugupanchangam.app";

/** Display hostname (no protocol) — used in OG cards and email footers. */
export const APP_HOST = "telugupanchangam.app";

/** Email address users can write to with festival/accuracy corrections. */
export const REPORT_EMAIL = "accuracy@telugupanchangam.app";

/** From-address for reminder emails sent via Resend. */
export const REMINDER_FROM_EMAIL = "reminders@telugupanchangam.app";

/** GitHub repository (owner/repo). */
export const GITHUB_REPO = "suhasatluri/telugu-panchangam-app";
